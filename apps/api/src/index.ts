/**
 * @brasso/api — API Fastify 5 + Zod (REST). Plugins auth/RBAC, modules par domaine, webhooks.
 *
 * Placeholder posé au ticket M0-01. Le squelette Fastify (config, error handler, /health)
 * arrive au ticket M0-05.
 */
export const PACKAGE_NAME = "@brasso/api" as const;
