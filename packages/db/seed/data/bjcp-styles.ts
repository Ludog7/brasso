// Styles BJCP — la source unique vit désormais dans `@brasso/core`
// (`packages/core/src/reference/bjcpStyles.ts`), réutilisée par le moteur BEER,
// l'API (référentiels éditeur, M2-04) et le front. Réexport ici pour le seed.
export { BJCP_STYLES, type BjcpStyle } from "@brasso/core";
